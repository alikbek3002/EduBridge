server {
    listen       ${PORT};
    server_name  _;

    root /usr/share/nginx/html;
    index index.html;
${API_PROXY_LOCATION}

    location / {
        try_files $uri $uri/ /index.html;
    }
}
