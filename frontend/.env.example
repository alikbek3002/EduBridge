# Frontend production runtime target
# Used by the client and by the Railway frontend nginx /api proxy fallback.
VITE_API_URL=https://your-backend.up.railway.app
# Leave VITE_API_URL empty only for localhost development.
